import { TableBodyCellDirective } from './table-body-cell.directive';

describe('TableBodyCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TableBodyCellDirective();
    expect(directive).toBeTruthy();
  });
});
